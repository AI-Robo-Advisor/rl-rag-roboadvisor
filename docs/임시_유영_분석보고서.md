# [임시][유영] RL 로보어드바이저 분석 보고서

> **작성자**: 강유영  
> **작성일**: 2026-05-22  
> **상태**: 임시 초안 — 모델 재학습 후 §3·§4·§5 수치 기입 필요  
> **참고 문서**: `docs/walk_forward_plan.md`, `docs/spec_tag_mapping.md`, `docs/rag_eval_questions.json`

---

## 0. 문서 목적

학습 → 백테스트 → ANOVA → SHAP → RAG 연동 결과를 리포트 작성 기준으로 정리한다.  
**현재 모델은 risk vector 연동(`set_risk_vector()`) 반영 전 구버전이므로, 수치 섹션은 재학습 완료 후 채운다.**

---

## 1. 실험 산출물 체크리스트

| 항목 | 경로 | 상태 |
|------|------|------|
| 로그수익률 | `data/processed/returns.parquet` (1898행, 10자산) | ✅ |
| 피처 | `data/processed/raw_features.parquet` (1865행, 40열) | ✅ |
| Scaler | `data/processed/scalers/{w1~final}_feature_stats.json` | ✅ 4개 |
| risk_vectors_daily | `data/processed/risk_vectors_daily.parquet` (2922일) | ✅ |
| regime_labels | `data/processed/regime_labels.csv` (1899행) | ✅ |
| **PPO 모델 (재학습)** | `models/ppo_{return\|sharpe\|mdd}_{w1~final}_risk.zip` | ❌ **이문정 재학습 대기 중** |
| 백테스트 CSV | `data/results/backtest_{return\|sharpe\|mdd}.csv` | ❌ 재학습 완료 후 강유영 재실행 |
| weights parquet | `data/results/weights_{return\|sharpe\|mdd}.parquet` | ❌ 재학습 완료 후 강유영 재실행 |
| SHAP summary | `data/results/shap_summary.png` | ❌ 재학습 완료 후 강유영 재실행 |
| SHAP force plot | `data/results/shap_force_*.html` | ❌ 재학습 완료 후 강유영 재실행 |
| TensorBoard 로그 | `logs/tensorboard/` | ❌ 이문정 담당 |

---

## 2. Walk-Forward 윈도우 정의

> **참조**: `docs/walk_forward_plan.md` · `src/rl/backtest.py:WINDOWS`

| window | 학습 기간 | 테스트 기간 | 시장 맥락 |
|--------|-----------|-------------|-----------|
| w1 | 2018-01-01 ~ 2021-12-31 | 2022-01-01 ~ 2022-12-31 | **금리 충격** — FOMC 자이언트 스텝(0.75%p) 연속 |
| w2 | 2019-01-01 ~ 2022-12-31 | 2023-01-01 ~ 2023-12-31 | **회복장** — 금리 인상 후 안정, 기술주 반등 |
| w3 | 2020-01-01 ~ 2023-12-31 | 2024-01-01 ~ 2024-12-31 | **AI 랠리** — 엔비디아·빅테크 주도 강세장 |
| final | 2021-01-01 ~ 2024-12-31 | 2025-01-01 ~ 2025-12-31 | **최신 holdout** — 관세·지정학 혼조 |

---

## 3. 백테스트 분석

> ⚠️ **모델 재학습 후 기입** — 현재 모델은 risk vector 연동 전 구버전

### 3-1. 표 A — Walk-Forward OOS 12지표 (전략 × 윈도우)

재학습 완료 후 아래 명령어로 생성:

```bash
python -m src.rl.backtest return
python -m src.rl.backtest sharpe
python -m src.rl.backtest mdd
```

| 전략 | w1 Sharpe | w2 Sharpe | w3 Sharpe | final Sharpe | w1 MDD | 비고 |
|------|-----------|-----------|-----------|--------------|--------|------|
| PPO-sharpe | — | — | — | — | — | 재학습 후 기입 |
| PPO-return | — | — | — | — | — | |
| PPO-mdd | — | — | — | — | — | |
| MVO | — | — | — | — | — | 거래비용 미포함 주의 |
| 동일비중 | — | — | — | — | — | |
| SPY | — | — | — | — | — | 벤치마크 |

### 3-2. 표 B — 윈도우별 해석

재학습 후 기입. 각 윈도우마다 아래 항목 서술:
1. 수익: 누적수익·CAGR·Alpha
2. 리스크: MDD·VaR·CVaR·Safe-Guard 발동 횟수
3. 위험조정: Sharpe·Sortino·Calmar
4. 한 줄 결론: 방어/공격/중립 성향

### 3-3. 표 C — 보상함수 최종 선택

재학습 후 기입. 선택 근거 4항목:
1. OOS Sharpe 우위 윈도우 수
2. MDD·Safe-Guard 발동 빈도
3. Alpha 안정성
4. TensorBoard 수렴 곡선 (이문정 산출물)

---

## 4. ANOVA 분석

> ⚠️ **모델 재학습 후 기입** — backtest CSV 재생성 후 실행

재학습 완료 후 아래 명령어로 생성:

```python
PYTHONPATH=. python -c "
from src.rl.anova import run_all_anova
import pandas as pd
results = run_all_anova(pd.read_parquet('data/processed/returns.parquet'))
for r in results:
    print(r['name'], r['f_statistic'], r['p_value'])
"
```

### 4-1. 표 D — ANOVA 요약

| 실험 | F | p | η² | 유의 (α=0.05) |
|------|---|---|----|--------------|
| reward_function_comparison | — | — | — | — |
| strategy_comparison | — | — | — | — |
| market_regime (regime 주효과) | — | — | — | — |
| market_regime (strategy 주효과) | — | — | — | — |
| market_regime (interaction) | — | — | — | — |

### 4-2. 표 E — Tukey 사후검정

재학습 후 기입.

> **작성 참고**: MVO는 거래비용 미포함이므로 strategy_comparison에서 MVO > PPO가 나와도 "이론적 상한선" 문구 병기 필요.

---

## 5. SHAP 분석

> ⚠️ **모델 재학습 후 기입** — risk vector 연동 후 재생성해야 `risk_*` 피처 등장

재학습 완료 후 아래 명령어로 생성:

```python
PYTHONPATH=. python -c "
from src.rl.shap import generate_force_plot
import pandas as pd
generate_force_plot('models/ppo_sharpe_final_risk.zip',
    pd.read_parquet('data/processed/raw_features.parquet'),
    pd.read_parquet('data/processed/returns.parquet'),
    date='2022-06-15')
"
```

### 5-1. 대표 시점 (재학습 후 분석 예정)

| 날짜 | 구분 | 시장 맥락 |
|------|------|-----------|
| 2022-06-15 | rate_hike | FOMC 자이언트 스텝 0.75%p (w1 OOS 대표) |
| 2024-02-29 | bull | AI 랠리 절정 (w3 OOS 대표) |

### 5-2. 표 F — 시점별 Top SHAP 기여 피처

재학습 후 기입. risk vector 연동 완료 시 `risk_macro_rate_risk`, `risk_equity_market_risk`, `risk_geopolitical_fx_risk` 피처 기여도 포함 예상.

### 5-3. RAG·리스크 벡터 연동 스토리

risk vector 연동(`train.py` set_risk_vector) 완료 후 기술:
1. `/research?question=...` → `risk_tags`
2. `risk_vectors_daily.parquet` 해당 날짜 값 (`risk_macro`, `risk_equity`, `risk_geo`)
3. SHAP에서 `risk_*` 기여도
4. "뉴스에서 탐지한 리스크가 그날 비중 축소에 기여했는지" 한 스토리

---

## 6. RAG 품질 평가

> `PYTHONPATH=. python scripts/rag_eval.py` 실행 결과 (ChromaDB 1,316건 기준)

| 지표 | 결과 |
|------|------|
| 검색 히트율 (cosine dist < 0.4) | **12/12 = 100%** |
| 태그 정확도 | **10/12 = 83.3%** |

실패 2건: Q08(인플레이션), Q11(한국은행 기준금리) — ECOS 통계 데이터 매칭 부족

**ChromaDB 구성**: FRED(733) + ECOS(455) + manual_seed(39) = 1,227건 + 실시간 RSS 89건

---

## 7. 한계 및 개선 사항

| 구분 | 내용 |
|------|------|
| 모델 재학습 대기 | risk vector 연동 후 재학습 필요 → §3·§4·§5 전부 미기입 |
| MVO 거래비용 | 미포함 → ANOVA strategy_comparison 해석 시 반드시 병기 |
| GDELT 라벨링 | 23,985건 이벤트 코드 형식(뉴스 본문 없음) → ChromaDB 제외 중 |
| RAG Q08·Q11 | ECOS 통계 데이터가 자연어 쿼리 매칭 약함 |
